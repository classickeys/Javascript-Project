{
  "name": "slots-machine",
  "version": "1.0.0",
  "description": "A slots machine game using javascript",
  "main": "project.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prompt-sync": "^4.2.0"
  }
}
